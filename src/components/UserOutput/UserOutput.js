import React from 'react';

const useroutput = props => {
  return (
    <div>
      <p>par one {props.username} </p>
      <p>par two {props.username} </p>
    </div>
  )
}

export default useroutput;